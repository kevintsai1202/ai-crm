# SP12-F + SP14 + SP15 統一實作計畫

> 接續 SP10–SP13 完成後的剩餘優化，**一次規劃、同一波交付**。  
> 金鑰輪替仍排除。

**Goal:** 收斂 AI 核心可維護性、前端模組邊界、基本限流與文件對齊。

**Tech:** Java 21 / Spring Boot 4.1 / React 19 / Vite / Vitest

## 範圍

| ID | 內容 | 完成定義 |
|----|------|----------|
| **SP12-F** | 抽出 `AiGroundingService`（context 組裝）+ `LlmInvocationHelper` 介面化同步呼叫記錄路徑；InsightService 改委派 | 既有 AI 單元測試綠；InsightService 依賴更清晰 |
| **SP14** | `frontend/src/api/*` 按 domain 拆檔 + `index.ts` re-export；`styles` 入口拆分；vitest 測 `format`/`layout` | tsc/build 綠；vitest ≥5 assert |
| **SP15** | 登入 + AI 寫入端點簡易限流；文件（README Agent 命名、roadmap、consulting 附錄） | 超限 429；文件不再過時宣稱 |

## 刻意不做（本波）

- 真 multi-agent tool-calling  
- Portfolio 全表 SQL 重寫  
- 完整 Actuator Prometheus 上線（可加輕量 counter log）  
- 金鑰輪替  

## 執行序（同 session）

1. SP12-F 後端抽出 → 跑 Insight 測試  
2. SP14 api 拆分 → tsc  
3. SP14 styles 最小拆 + vitest  
4. SP15 RateLimitFilter + 文件  
5. 回歸測試 + 單 commit 或分 commit  

---
