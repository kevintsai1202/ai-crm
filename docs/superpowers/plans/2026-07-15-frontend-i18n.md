# Frontend i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 ai-crm 前端建立 react-i18next 基建（en/zh-TW），並完整遷移 LoginPage 作為試點，含登入頁與側邊欄兩處語言切換。

**Architecture:** 以 react-i18next 預設全域實例在 `main.tsx` 一次初始化；語言偵測用 `i18next-browser-languagedetector`（localStorage → navigator），fallback en。翻譯資源用單一 `common` namespace 的 en/zh-TW JSON。`detect.ts` 為可測試純函式，與 detector 行為對齊。

**Tech Stack:** React 19 + TypeScript + Vite；i18next / react-i18next / i18next-browser-languagedetector；Vitest + @testing-library/react（新增 jsdom 環境）。

## Global Constraints

- 套件管理用 **pnpm**；Git Bash 環境每個指令前先 `export PATH="/d/nodejs:$PATH"`。
- 每個 task 完成前後皆須 `pnpm exec tsc --noEmit` exit 0。
- 所有新函式需**中文函式級註解**（`/** ... */`），重要變數加中文行內註解（依 `frontend/CLAUDE.md`）。
- 支援語言固定 `["en", "zh-TW"]`；fallback/預設主語言為 **en**。
- localStorage 語言 key 固定為 `ai-crm-lang`。
- 單一 namespace 名稱固定為 `common`。
- 所有工作在分支 `feat/frontend-i18n`（已建立）上進行。
- 不修改 `lib/format.ts`、不遷移 LoginPage 以外頁面（本次範圍外）。

---

## File Structure

- Create: `frontend/src/i18n/detect.ts` — `detectLanguage` 純函式 + 語言常數
- Create: `frontend/src/i18n/detect.test.ts` — detect 單元測試（node 環境）
- Create: `frontend/src/i18n/index.ts` — i18next 初始化（唯一初始化點）
- Create: `frontend/src/i18n/locales/en/common.json` — 英文資源
- Create: `frontend/src/i18n/locales/zh-TW/common.json` — 繁中資源
- Create: `frontend/src/test/setup.ts` — 測試 setup（jest-dom + cleanup）
- Create: `frontend/src/components/common/LanguageSwitcher.tsx` — 語言切換元件
- Create: `frontend/src/components/common/LanguageSwitcher.test.tsx` — 切換元件測試
- Create: `frontend/src/features/auth/LoginPage.test.tsx` — LoginPage i18n 測試
- Create: `frontend/src/app/AppShell.test.tsx` — AppShell 切換鈕整合測試
- Modify: `frontend/vitest.config.ts` — 改 jsdom、收 `.tsx`、加 setup、加 react plugin
- Modify: `frontend/src/main.tsx:1-6` — 加 `import "./i18n"`
- Modify: `frontend/src/features/auth/LoginPage.tsx` — 文字改 `t(...)` + 放入切換元件
- Modify: `frontend/src/app/AppShell.tsx` — 側邊欄放入切換元件

---

## Task 1: detectLanguage 純函式

**Files:**
- Create: `frontend/src/i18n/detect.ts`
- Test: `frontend/src/i18n/detect.test.ts`

**Interfaces:**
- Consumes: 無
- Produces:
  - `type SupportedLang = "en" | "zh-TW"`
  - `const SUPPORTED_LANGS: SupportedLang[]`（值 `["en", "zh-TW"]`）
  - `const FALLBACK_LANG: SupportedLang`（值 `"en"`）
  - `function detectLanguage(browserLang: string | undefined, stored: string | null): SupportedLang`

- [ ] **Step 1: 寫失敗測試**

`frontend/src/i18n/detect.test.ts`：
```ts
import { describe, it, expect } from "vitest";
import { detectLanguage } from "./detect";

describe("detectLanguage", () => {
  // 已存 localStorage 選擇時，優先採用（且忽略瀏覽器語系）
  it.each([
    ["en", "zh-TW", "zh-TW"],
    ["zh-TW", "en", "en"],
  ])("stored=%s 覆蓋 browser=%s → %s", (browser, stored, expected) => {
    expect(detectLanguage(browser, stored)).toBe(expected);
  });

  // 無 stored（或 stored 非法）時，依瀏覽器語系判斷；zh* → zh-TW，其餘 → en
  it.each([
    ["zh-TW", null, "zh-TW"],
    ["zh-CN", null, "zh-TW"],
    ["zh-HK", null, "zh-TW"],
    ["en-US", null, "en"],
    ["fr", null, "en"],
    [undefined, null, "en"],
    ["zh-TW", "invalid", "zh-TW"],
  ])("browser=%s stored=%s → %s", (browser, stored, expected) => {
    expect(detectLanguage(browser as string | undefined, stored)).toBe(expected);
  });
});
```

- [ ] **Step 2: 執行確認失敗**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- detect`
Expected: FAIL（`Cannot find module './detect'` 或 detectLanguage is not a function）

- [ ] **Step 3: 實作最小版本**

`frontend/src/i18n/detect.ts`：
```ts
/** 前端支援的語言碼。 */
export type SupportedLang = "en" | "zh-TW";

/** 支援語言清單（供 detector、切換元件、初始化共用）。 */
export const SUPPORTED_LANGS: SupportedLang[] = ["en", "zh-TW"];

/** fallback / 預設主語言。 */
export const FALLBACK_LANG: SupportedLang = "en";

/**
 * 決定初始語言：優先採用已儲存的合法選擇，否則依瀏覽器語系（zh* → zh-TW，其餘 → en）。
 * 與 i18next-browser-languagedetector 的行為對齊，並可獨立單元測試。
 * @param browserLang 瀏覽器語系字串（如 navigator.language）
 * @param stored localStorage 已存語言（可能為 null 或非法值）
 */
export function detectLanguage(browserLang: string | undefined, stored: string | null): SupportedLang {
  // 已存合法選擇最優先
  if (stored === "en" || stored === "zh-TW") return stored;
  // 依瀏覽器語系判斷：中文各變體一律對映繁中，其餘皆英文
  const lang = (browserLang ?? "").toLowerCase();
  if (lang.startsWith("zh")) return "zh-TW";
  return FALLBACK_LANG;
}
```

- [ ] **Step 4: 執行確認通過**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- detect`
Expected: PASS（9 案例全過）

- [ ] **Step 5: 型別檢查**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm exec tsc --noEmit`
Expected: exit 0

- [ ] **Step 6: Commit**

```bash
git add frontend/src/i18n/detect.ts frontend/src/i18n/detect.test.ts
git commit -m "feat(i18n): add detectLanguage pure function with tests"
```

---

## Task 2: i18n 初始化 + locale 資源 + main.tsx 接線

**Files:**
- Create: `frontend/src/i18n/locales/en/common.json`
- Create: `frontend/src/i18n/locales/zh-TW/common.json`
- Create: `frontend/src/i18n/index.ts`
- Modify: `frontend/src/main.tsx:1-6`

**Interfaces:**
- Consumes: `SUPPORTED_LANGS`, `FALLBACK_LANG`（Task 1）
- Produces: `export default i18n`（已 init 的 i18next 實例，`defaultNS: "common"`）

- [ ] **Step 1: 安裝相依**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm add i18next react-i18next i18next-browser-languagedetector`
Expected: 三套件寫入 `package.json` dependencies

- [ ] **Step 2: 建立英文資源**

`frontend/src/i18n/locales/en/common.json`：
```json
{
  "lang": { "label": "Language", "en": "English", "zh-TW": "繁體中文" },
  "login": {
    "badge": "Unit 4 + Unit 5",
    "title": "Sign in to AI CRM Workbench",
    "intro": "Use the seed accounts to enter the full workbench and verify JWT, role permissions, Dashboard, customer data, AI assistant, and Agent Trace.",
    "username": "Username",
    "password": "Password",
    "submit": "Sign in",
    "accounts": "Available accounts: sales@aurora.local / manager@aurora.local / admin@aurora.local, password is password123.",
    "surveyLink": "📋 Fill out the fundraising course survey",
    "error": {
      "invalid": "Incorrect username or password.",
      "rateLimited": "Too many requests, please try again later.",
      "failedDetail": "Login failed: {{detail}}",
      "noBackend": "Cannot reach backend, please confirm the service is running (18080) and the frontend proxy works.",
      "generic": "Login failed, please check your credentials."
    }
  }
}
```

- [ ] **Step 3: 建立繁中資源**

`frontend/src/i18n/locales/zh-TW/common.json`：
```json
{
  "lang": { "label": "語言", "en": "English", "zh-TW": "繁體中文" },
  "login": {
    "badge": "Unit 4 + Unit 5",
    "title": "登入 AI CRM 工作台",
    "intro": "使用教學 seed 帳號進入完整工作台，驗證 JWT、角色權限、Dashboard、客戶資料、AI 助理與 Agent Trace。",
    "username": "帳號",
    "password": "密碼",
    "submit": "登入",
    "accounts": "可用帳號：sales@aurora.local / manager@aurora.local / admin@aurora.local，密碼皆為 password123。",
    "surveyLink": "📋 填寫募資課程問卷",
    "error": {
      "invalid": "帳號或密碼錯誤。",
      "rateLimited": "請求過於頻繁，請稍後再試。",
      "failedDetail": "登入失敗：{{detail}}",
      "noBackend": "無法連線後端，請確認服務已啟動（18080）且前端代理正常。",
      "generic": "登入失敗，請確認帳號與密碼。"
    }
  }
}
```

- [ ] **Step 4: 建立初始化模組**

`frontend/src/i18n/index.ts`：
```ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import LanguageDetector from "i18next-browser-languagedetector";
import en from "./locales/en/common.json";
import zhTW from "./locales/zh-TW/common.json";
import { SUPPORTED_LANGS, FALLBACK_LANG } from "./detect";

/**
 * i18next 全域初始化（整個 app 只在此執行一次，由 main.tsx import 觸發）。
 * 資源以靜態 import 打包，無非同步載入，故關閉 Suspense 以免需要額外 Suspense 邊界。
 */
void i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { common: en },
      "zh-TW": { common: zhTW }
    },
    fallbackLng: FALLBACK_LANG,
    supportedLngs: SUPPORTED_LANGS,
    defaultNS: "common",
    interpolation: { escapeValue: false }, // React 已防 XSS
    detection: {
      order: ["localStorage", "navigator"],
      lookupLocalStorage: "ai-crm-lang",
      caches: ["localStorage"]
    },
    react: { useSuspense: false }
  });

export default i18n;
```

- [ ] **Step 5: 在 main.tsx 接線**

修改 `frontend/src/main.tsx`，於 import 區塊加入一行（在 `import "./styles.css";` 之後）：
```ts
import "./styles.css";
import "./i18n"; // 觸發 i18next 全域初始化（須早於首次 render）
```

- [ ] **Step 6: 型別檢查 + build**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm exec tsc --noEmit && pnpm build`
Expected: 兩者皆 exit 0（build 產出 dist）

- [ ] **Step 7: Commit**

```bash
git add frontend/src/i18n/index.ts frontend/src/i18n/locales frontend/src/main.tsx frontend/package.json frontend/pnpm-lock.yaml
git commit -m "feat(i18n): init react-i18next with en/zh-TW resources"
```

---

## Task 3: 元件測試基建 + LanguageSwitcher

**Files:**
- Modify: `frontend/vitest.config.ts`
- Create: `frontend/src/test/setup.ts`
- Create: `frontend/src/components/common/LanguageSwitcher.tsx`
- Test: `frontend/src/components/common/LanguageSwitcher.test.tsx`

**Interfaces:**
- Consumes: `SUPPORTED_LANGS`（Task 1）、`i18n` 預設實例（Task 2）
- Produces: `function LanguageSwitcher(props: { className?: string }): JSX.Element`（具名匯出）

- [ ] **Step 1: 安裝測試相依**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm add -D @testing-library/react @testing-library/jest-dom jsdom`
Expected: 三套件寫入 devDependencies

- [ ] **Step 2: 更新 vitest 設定為 jsdom**

覆寫 `frontend/vitest.config.ts`：
```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

/**
 * Vitest 設定：改用 jsdom 以支援元件測試（含 i18n 初始化需要的 window/localStorage）。
 * 收錄 .ts 與 .tsx 測試；setup 檔註冊 jest-dom matcher 與每次測試後 cleanup。
 */
export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    include: ["src/**/*.test.{ts,tsx}"],
    setupFiles: ["./src/test/setup.ts"]
  }
});
```

- [ ] **Step 3: 建立測試 setup**

`frontend/src/test/setup.ts`：
```ts
import "@testing-library/jest-dom/vitest";
import { afterEach } from "vitest";
import { cleanup } from "@testing-library/react";

// 每個測試結束後卸載渲染結果，避免 DOM 殘留互相污染
afterEach(() => {
  cleanup();
});
```

- [ ] **Step 4: 確認既有測試在新環境仍通過**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test`
Expected: PASS（既有 `format.test.ts`、`layout.test.ts`、`detect.test.ts` 在 jsdom 下全過）

- [ ] **Step 5: 寫失敗測試**

`frontend/src/components/common/LanguageSwitcher.test.tsx`：
```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect, beforeEach } from "vitest";
import i18n from "../../i18n";
import { LanguageSwitcher } from "./LanguageSwitcher";

describe("LanguageSwitcher", () => {
  beforeEach(async () => {
    localStorage.clear();
    await i18n.changeLanguage("en");
  });

  it("顯示兩個語言選項", () => {
    render(<LanguageSwitcher />);
    expect(screen.getByRole("option", { name: "English" })).toBeInTheDocument();
    expect(screen.getByRole("option", { name: "繁體中文" })).toBeInTheDocument();
  });

  it("切換語言會更新 i18n 並寫入 localStorage", () => {
    render(<LanguageSwitcher />);
    fireEvent.change(screen.getByRole("combobox"), { target: { value: "zh-TW" } });
    expect(i18n.resolvedLanguage).toBe("zh-TW");
    expect(localStorage.getItem("ai-crm-lang")).toBe("zh-TW");
  });
});
```

- [ ] **Step 6: 執行確認失敗**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- LanguageSwitcher`
Expected: FAIL（`Cannot find module './LanguageSwitcher'`）

- [ ] **Step 7: 實作元件**

`frontend/src/components/common/LanguageSwitcher.tsx`：
```tsx
import { useTranslation } from "react-i18next";
import { SUPPORTED_LANGS } from "../../i18n/detect";

/**
 * 語言切換下拉：切換 i18next 語言，變更會由 languageDetector 自動寫回 localStorage(ai-crm-lang)。
 * 用原生 <select> 以確保無障礙且不引入額外樣式負擔。
 * @param className 供外層版面調整用的樣式類別
 */
export function LanguageSwitcher({ className }: { className?: string }) {
  const { t, i18n } = useTranslation();
  return (
    <select
      className={className}
      aria-label={t("lang.label")}
      value={i18n.resolvedLanguage}
      onChange={(event) => {
        void i18n.changeLanguage(event.target.value);
      }}
    >
      {SUPPORTED_LANGS.map((lng) => (
        <option key={lng} value={lng}>
          {t(`lang.${lng}`)}
        </option>
      ))}
    </select>
  );
}
```

- [ ] **Step 8: 執行確認通過 + 型別檢查**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- LanguageSwitcher && pnpm exec tsc --noEmit`
Expected: 測試 PASS + tsc exit 0

- [ ] **Step 9: Commit**

```bash
git add frontend/vitest.config.ts frontend/src/test/setup.ts frontend/src/components/common/LanguageSwitcher.tsx frontend/src/components/common/LanguageSwitcher.test.tsx frontend/package.json frontend/pnpm-lock.yaml
git commit -m "feat(i18n): add LanguageSwitcher + jsdom test infra"
```

---

## Task 4: LoginPage 遷移（試點）

**Files:**
- Modify: `frontend/src/features/auth/LoginPage.tsx`
- Test: `frontend/src/features/auth/LoginPage.test.tsx`

**Interfaces:**
- Consumes: `LanguageSwitcher`（Task 3）、`i18n`（Task 2）、`useTranslation`
- Produces: 無新對外介面（維持 `export function LoginPage()`）

- [ ] **Step 1: 寫失敗測試**

`frontend/src/features/auth/LoginPage.test.tsx`：
```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect, beforeEach, vi } from "vitest";
import { MemoryRouter } from "react-router-dom";
import i18n from "../../i18n";
import { LoginPage } from "./LoginPage";

// 隔離 AuthContext：試點測試只驗證 i18n 文字，不觸發真實登入/網路
vi.mock("../../context/AuthContext", () => ({
  useAuth: () => ({ isAuthed: false, login: vi.fn() })
}));

describe("LoginPage i18n", () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it("預設英文顯示 Sign in 標題與按鈕", async () => {
    await i18n.changeLanguage("en");
    render(
      <MemoryRouter>
        <LoginPage />
      </MemoryRouter>
    );
    expect(screen.getByRole("heading", { name: /Sign in to AI CRM/i })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "Sign in" })).toBeInTheDocument();
  });

  it("切換繁中顯示登入按鈕", async () => {
    await i18n.changeLanguage("zh-TW");
    render(
      <MemoryRouter>
        <LoginPage />
      </MemoryRouter>
    );
    expect(screen.getByRole("button", { name: "登入" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 執行確認失敗**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- LoginPage`
Expected: FAIL（預設英文時找不到 "Sign in" 按鈕，因目前為硬編碼中文「登入」）

- [ ] **Step 3: 遷移 LoginPage**

覆寫 `frontend/src/features/auth/LoginPage.tsx`：
```tsx
import { FormEvent, useState } from "react";
import { Navigate, useNavigate } from "react-router-dom";
import { useTranslation } from "react-i18next";
import { useAuth } from "../../context/AuthContext";
import { LanguageSwitcher } from "../../components/common/LanguageSwitcher";

/**
 * 登入頁：已登入自動導向儀表板，否則顯示教學帳號登入表單。
 * i18n 試點頁：所有可視文字改由 t() 取得，右上角提供語言切換。
 */
export function LoginPage() {
  const { login, isAuthed } = useAuth();
  const { t } = useTranslation();
  const navigate = useNavigate();
  const [error, setError] = useState("");

  // 已登入則直接導向儀表板
  if (isAuthed) return <Navigate to="/dashboard" replace />;

  /** 處理登入表單送出。 */
  async function handleLogin(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setError("");
    const form = new FormData(event.currentTarget);
    try {
      await login(String(form.get("username")), String(form.get("password")));
      navigate("/dashboard", { replace: true });
    } catch (e: unknown) {
      // 顯示較具體錯誤（網路／HTTP），方便本機除錯
      const ax = e as { response?: { status?: number; data?: { detail?: string } }; message?: string };
      const detail = ax?.response?.data?.detail;
      const status = ax?.response?.status;
      if (status === 401) {
        setError(t("login.error.invalid"));
      } else if (status === 429) {
        setError(t("login.error.rateLimited"));
      } else if (detail) {
        setError(t("login.error.failedDetail", { detail }));
      } else if (!ax?.response) {
        setError(t("login.error.noBackend"));
      } else {
        setError(t("login.error.generic"));
      }
    }
  }

  return (
    <section className="login-panel">
      <div className="login-copy">
        {/* 語言切換：登入為公開頁，未登入者亦可切換介面語言 */}
        <LanguageSwitcher className="lang-switcher" />
        <span>{t("login.badge")}</span>
        <h2>{t("login.title")}</h2>
        <p>{t("login.intro")}</p>
      </div>
      <form className="login-form" onSubmit={handleLogin}>
        <label>
          {t("login.username")}
          <input name="username" defaultValue="sales@aurora.local" autoComplete="username" />
        </label>
        <label>
          {t("login.password")}
          <input name="password" type="password" defaultValue="password123" autoComplete="current-password" />
        </label>
        {error ? <div className="error-box">{error}</div> : null}
        <button type="submit">{t("login.submit")}</button>
        <small>{t("login.accounts")}</small>
        {/* 募資課程問卷：新分頁開啟，避免離開登入流程時遺失表單狀態 */}
        <a
          className="survey-link"
          href="https://survey.springai.world/"
          target="_blank"
          rel="noopener noreferrer"
        >
          {t("login.surveyLink")}
        </a>
      </form>
    </section>
  );
}
```

- [ ] **Step 4: 執行確認通過**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- LoginPage`
Expected: PASS（兩案例皆過）

- [ ] **Step 5: 全測試 + 型別檢查 + build**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build`
Expected: 全綠 + exit 0

- [ ] **Step 6: Commit**

```bash
git add frontend/src/features/auth/LoginPage.tsx frontend/src/features/auth/LoginPage.test.tsx
git commit -m "feat(i18n): migrate LoginPage to react-i18next (pilot)"
```

---

## Task 5: AppShell 側邊欄整合切換元件

**Files:**
- Modify: `frontend/src/app/AppShell.tsx`
- Test: `frontend/src/app/AppShell.test.tsx`

**Interfaces:**
- Consumes: `LanguageSwitcher`（Task 3）
- Produces: 無新對外介面

- [ ] **Step 1: 寫失敗測試**

`frontend/src/app/AppShell.test.tsx`：
```tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";
import { MemoryRouter } from "react-router-dom";
import { AppShell } from "./AppShell";

// 隔離 auth 與版本輪詢，避免測試觸發網路
vi.mock("../context/AuthContext", () => ({
  useAuth: () => ({
    user: { displayName: "Sales", role: "SALES" },
    health: null,
    healthError: false,
    refreshHealth: vi.fn(),
    logout: vi.fn()
  })
}));
vi.mock("../hooks/useVersionCheck", () => ({
  useVersionCheck: () => false
}));

describe("AppShell", () => {
  it("側邊欄含語言切換下拉", () => {
    render(
      <MemoryRouter>
        <AppShell />
      </MemoryRouter>
    );
    expect(screen.getByRole("combobox")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 執行確認失敗**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- AppShell`
Expected: FAIL（找不到 combobox，因側邊欄尚無切換元件）

- [ ] **Step 3: 在 AppShell 側邊欄放入切換元件**

修改 `frontend/src/app/AppShell.tsx`：

(a) 於檔案頂部 import 區塊加入：
```ts
import { LanguageSwitcher } from "../components/common/LanguageSwitcher";
```

(b) 將側邊欄底部的 user-card 區塊替換為（在 user-card 後補上語言切換）：
```tsx
        {user ? (
          <div className="user-card">
            <strong>{user.displayName}</strong>
            <span>{user.role}</span>
            <button type="button" onClick={logout}>登出</button>
          </div>
        ) : null}
        {/* 語言切換置於側邊欄底部，登入後全站可切換介面語言 */}
        <LanguageSwitcher className="lang-switcher" />
```

- [ ] **Step 4: 執行確認通過**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test -- AppShell`
Expected: PASS

- [ ] **Step 5: 全測試 + 型別檢查 + build**

Run: `export PATH="/d/nodejs:$PATH"; cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build`
Expected: 全綠 + exit 0

- [ ] **Step 6: Commit**

```bash
git add frontend/src/app/AppShell.tsx frontend/src/app/AppShell.test.tsx
git commit -m "feat(i18n): add LanguageSwitcher to AppShell sidebar"
```

---

## 驗收（人工/e2e）

- `pnpm dev` 後開啟 `/login`：預設瀏覽器非中文時應為英文；右上角下拉可切 en/zh-TW，重整後保留選擇（localStorage `ai-crm-lang`）。
- 登入後側邊欄底部下拉同樣可切換，全站語言一致。
- 既有 Playwright 煙霧測試（`pnpm exec playwright test`，需後端 18080）不因文字變動而破壞——若 e2e 斷言硬編碼中文「登入」，需同步調整或設定其預設語言為 zh-TW（列為執行時檢查項）。

## 備註：日後分批（本次不做）

- `lib/format.ts` 列舉標籤函式改回傳 i18n key 或吃 `t`；`Intl` locale 改讀 `i18n.language`。
- 其餘 59 個含中文檔案依 feature 逐頁遷移，遷移時擴充對應 namespace（如 `dashboard`、`customers`）。
